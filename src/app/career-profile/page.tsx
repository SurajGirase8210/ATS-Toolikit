'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {Plus,Trash2,Save,BriefcaseBusiness} from 'lucide-react';import toast from 'react-hot-toast';import {CareerProfile,emptyCareerProfile,loadCareerProfile,saveCareerProfile} from '@/lib/career-profile';import {SAMPLE_EVIDENCE} from '@/lib/sample-data';
export default function CareerProfilePage(){const[p,setP]=useState<CareerProfile>(emptyCareerProfile);useEffect(()=>setP(loadCareerProfile()),[]);const loadSample=()=>{setP({name:'Sample Candidate',headline:'Entry-Level Data Analyst',contact:'sample@example.com · +91 00000 00000 · linkedin.com/in/sample · github.com/sample',items:SAMPLE_EVIDENCE});toast.success('Sample evidence loaded.');};const add=()=>setP(x=>({...x,items:[...x.items,{id:crypto.randomUUID(),type:'achievement',title:'',content:'',tags:[],enabled:true}]}));const update=(id:string,k:string,v:any)=>setP(x=>({...x,items:x.items.map(i=>i.id===id?{...i,[k]:v}:i)}));return <div className="container py-10"><div className="flex items-end justify-between gap-4"><div><span className="rounded-full bg-blue-100 px-3 py-1 text-xs font-bold text-blue-700">Master career profile</span><h1 className="mt-4 text-4xl font-black">Your evidence library</h1><p className="mt-2 max-w-3xl text-slate-600">Store truthful skills, achievements and bullets once. Tailored resumes can reuse them without inventing experience.</p></div><div className="flex flex-wrap gap-2"><button onClick={loadSample} className="rounded-xl border bg-white px-4 py-2.5 text-sm font-bold hover:bg-slate-50">Load sample data</button><Link href="/resume-builder" className="rounded-xl border bg-white px-4 py-2.5 font-bold">Open Builder</Link></div></div><div className="mt-8 grid gap-5 lg:grid-cols-3"><section className="rounded-2xl border bg-white p-5 lg:col-span-1"><h2 className="font-black">Identity</h2><input value={p.name} onChange={e=>setP({...p,name:e.target.value})} placeholder="Full name" className="mt-3 w-full rounded-xl border p-3"/><input value={p.headline} onChange={e=>setP({...p,headline:e.target.value})} placeholder="Target roles / headline" className="mt-3 w-full rounded-xl border p-3"/><textarea value={p.contact} onChange={e=>setP({...p,contact:e.target.value})} placeholder="Email · Phone · LinkedIn · GitHub" rows={4} className="mt-3 w-full rounded-xl border p-3"/><button onClick={()=>{saveCareerProfile(p);toast.success('Career profile saved.')}} className="mt-4 inline-flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-2.5 font-bold text-white"><Save size={16}/>Save Profile</button></section><section className="rounded-2xl border bg-white p-5 lg:col-span-2"><div className="flex items-center justify-between"><div><h2 className="font-black">Evidence items</h2><p className="text-xs text-slate-500">Tag items so tailoring can rank the most relevant evidence.</p></div><button onClick={add} className="inline-flex items-center gap-2 rounded-xl border px-3 py-2 text-sm font-bold"><Plus size={16}/> Add evidence</button></div><div className="mt-4 space-y-3">{p.items.map(i=><div key={i.id} className="rounded-xl border p-4"><div className="flex gap-2"><select value={i.type} onChange={e=>update(i.id,'type',e.target.value)} className="rounded-lg border p-2 text-sm"><option>skill</option><option>achievement</option><option>bullet</option><option>project</option><option>certification</option><option>experience</option></select><input value={i.title} onChange={e=>update(i.id,'title',e.target.value)} placeholder="Title" className="flex-1 rounded-lg border p-2"/><button onClick={()=>setP(x=>({...x,items:x.items.filter(z=>z.id!==i.id)}))} className="rounded-lg p-2 text-red-600"><Trash2 size={16}/></button></div><textarea value={i.content} onChange={e=>update(i.id,'content',e.target.value)} placeholder="Truthful evidence, bullet or achievement" rows={3} className="mt-2 w-full rounded-lg border p-2 text-sm"/><input value={i.tags.join(', ')} onChange={e=>update(i.id,'tags',e.target.value.split(',').map(s=>s.trim()).filter(Boolean))} placeholder="Tags: SQL, Power BI, stakeholder management" className="mt-2 w-full rounded-lg border p-2 text-sm"/><label className="mt-2 flex items-center gap-2 text-xs"><input type="checkbox" checked={i.enabled} onChange={e=>update(i.id,'enabled',e.target.checked)}/> Available for tailoring</label></div>)}{!p.items.length&&<div className="rounded-xl bg-slate-50 p-8 text-center text-sm text-slate-500"><BriefcaseBusiness className="mx-auto mb-2"/>Add your strongest evidence here.</div>}</div></section></div></div>}
