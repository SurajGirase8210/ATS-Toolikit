import React from 'react';export default function TextArea({label,showCount,maxLength,...props}:{label?:string;showCount?:boolean;maxLength?:number}&React.TextareaHTMLAttributes<HTMLTextAreaElement>){return <label className="block space-y-1.5">{label&&<span className="text-sm font-medium text-slate-700">{label}</span>}<textarea {...props} maxLength={maxLength} className="min-h-40 w-full resize-y rounded-lg border border-slate-300 bg-white p-3 outline-none focus:border-blue-500 focus:ring-2 focus:ring-blue-100"/>{showCount&&<span className="text-xs text-slate-500">{String(props.value||'').length}{maxLength?` / ${maxLength}`:''}</span>}</label>}
